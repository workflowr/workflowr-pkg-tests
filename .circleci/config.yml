version: 2
jobs:
  future:
    docker:
      - image: rocker/r-ver:devel
    environment:
      NOT_CRAN: true
      R_LIBS: ~/R/Library
    steps:
      - restore_cache:
          keys:
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-
      - checkout
      - run:
          name: Install system dependencies
          command: |
            apt-get update
            apt-get install --yes --no-install-recommends \
              git \
              libcurl4-openssl-dev \
              libgit2-dev \
              libssl-dev \
              libxml2-dev \
              pandoc \
              pandoc-citeproc \
              qpdf \
              zlib1g-dev
      - run:
          name: Install package dependencies from GitHub
          command: |
            mkdir -p ~/R/Library
            Rscript -e 'install.packages("remotes")'
            Rscript -e 'remotes::install_github("r-lib/callr")'
            Rscript -e 'remotes::install_github("mdlincoln/clipr")'
            Rscript -e 'remotes::install_github("r-lib/covr")'
            Rscript -e 'remotes::install_github("r-lib/devtools")'
            Rscript -e 'remotes::install_github("r-lib/fs")'
            Rscript -e 'remotes::install_github("wrathematics/getPass")'
            Rscript -e 'remotes::install_github("ropensci/git2r")'
            Rscript -e 'remotes::install_github("tidyverse/glue")'
            Rscript -e 'remotes::install_github("r-lib/httr")'
            Rscript -e 'remotes::install_github("yihui/knitr")'
            Rscript -e 'remotes::install_github("rstudio/miniUI")'
            Rscript -e 'remotes::install_github("rstudio/rmarkdown")'
            Rscript -e 'remotes::install_github("krlmlr/rprojroot")'
            Rscript -e 'remotes::install_github("rstudio/rstudioapi")'
            Rscript -e 'remotes::install_github("rstudio/shiny")'
            Rscript -e 'remotes::install_github("ropensci/spelling")'
            Rscript -e 'remotes::install_github("tidyverse/stringr")'
            Rscript -e 'remotes::install_github("r-lib/testthat")'
            Rscript -e 'remotes::install_github("edwindj/whisker")'
            Rscript -e 'remotes::install_github("r-lib/withr")'
            Rscript -e 'remotes::install_github("viking/r-yaml")'
      - run:
          name: Clone and install workflowr
          command: |
            git clone https://github.com/jdblischak/workflowr.git ../workflowr
            R CMD build --no-build-vignettes --no-manual ../workflowr
            R CMD INSTALL workflowr_*tar.gz
      - run:
          name: Session information and installed package versions
          command: |
            Rscript -e 'library(workflowr); devtools::session_info()'
            Rscript -e 'installed.packages()[, c("Package", "Version")]'
            Rscript -e 'rmarkdown::pandoc_version()'
      - run:
          name: Build and check package
          command: |
            cd ../workflowr
            bash build.sh --no-manual
      - store_artifacts:
          path: ../workflowr/workflowr.Rcheck/
      - save_cache:
          key: r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
          paths:
            - "~/R/Library"
  devel:
    environment:
      NOT_CRAN: true
      PANDOC: 2.7
      R_LIBS: ~/R/Library
    docker:
      - image: rocker/r-ver:devel
    steps:
      - restore_cache:
          keys:
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-
      - checkout
      - run:
          name: Install system dependencies
          command: bash scripts/install-system.sh
      - run:
          name: Install pandoc
          command: bash scripts/install-pandoc.sh $PANDOC
      - run:
          name: Clone workflowr
          command: git clone https://github.com/jdblischak/workflowr.git /tmp/workflowr
      - run:
          name: Install package dependencies
          command: |
            mkdir -p ~/R/Library
            Rscript scripts/install-r-pkgs.R
      - run:
          name: Session information and installed package versions
          command: Rscript scripts/session-info.R
      - run:
          name: Build and check
          command: bash scripts/check.sh
      - store_artifacts:
          path: /tmp/workflowr/workflowr.Rcheck/
      - save_cache:
          key: r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
          paths:
            - "~/R/Library"
  r361:
    environment:
      NOT_CRAN: true
      PANDOC: 2.7
      R_LIBS: ~/R/Library
    docker:
      - image: rocker/r-ver:3.6.1
    steps:
      - restore_cache:
          keys:
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-
      - checkout
      - run:
          name: Install system dependencies
          command: bash scripts/install-system.sh
      - run:
          name: Install pandoc
          command: bash scripts/install-pandoc.sh $PANDOC
      - run:
          name: Clone workflowr
          command: git clone https://github.com/jdblischak/workflowr.git /tmp/workflowr
      - run:
          name: Install package dependencies
          command: |
            mkdir -p ~/R/Library
            Rscript scripts/install-r-pkgs.R
      - run:
          name: Session information and installed package versions
          command: Rscript scripts/session-info.R
      - run:
          name: Build and check
          command: bash scripts/check.sh
      - store_artifacts:
          path: /tmp/workflowr/workflowr.Rcheck/
      - save_cache:
          key: r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
          paths:
            - "~/R/Library"
  r353:
    environment:
      NOT_CRAN: true
      PANDOC: 2.7
      R_LIBS: ~/R/Library
    docker:
      - image: rocker/r-ver:3.5.3
    steps:
      - restore_cache:
          keys:
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-
      - checkout
      - run:
          name: Install system dependencies
          command: bash scripts/install-system.sh
      - run:
          name: Install pandoc
          command: bash scripts/install-pandoc.sh $PANDOC
      - run:
          name: Clone workflowr
          command: git clone https://github.com/jdblischak/workflowr.git /tmp/workflowr
      - run:
          name: Install package dependencies
          command: |
            mkdir -p ~/R/Library
            Rscript scripts/install-r-pkgs.R
      # Hot fix required b/c getOption("repos") is set to https://mran.microsoft.com/snapshot/2019-04-26
      - run:
          name: Install latest version of git2r
          command: Rscript -e 'install.packages("git2r", repos = "https://cran.rstudio.com/", dependencies = FALSE)'
      - run:
          name: Install clipr 0.7.0
          command: Rscript -e 'remotes::install_version("clipr", "0.7.0", repos = "https://cran.rstudio.com/", dependencies = FALSE)'
      - run:
          name: Session information and installed package versions
          command: Rscript scripts/session-info.R
      - run:
          name: Build and check
          command: bash scripts/check.sh
      - store_artifacts:
          path: /tmp/workflowr/workflowr.Rcheck/
      - save_cache:
          key: r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
          paths:
            - "~/R/Library"
  r325:
    environment:
      NOT_CRAN: true
      PANDOC: 2.7
      R_LIBS: ~/R/Library
    docker:
      - image: rocker/r-ver:3.2.5
    steps:
      - restore_cache:
          keys:
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-
      - checkout
      - run:
          name: Install system dependencies
          command: bash scripts/install-system.sh
      - run:
          name: Install pandoc
          command: bash scripts/install-pandoc.sh $PANDOC
      - run:
          name: Clone workflowr
          command: git clone https://github.com/jdblischak/workflowr.git /tmp/workflowr
      - run:
          name: Install package dependencies (not in old MRAN snapshot, or too outdated)
          command: |
            mkdir -p ~/R/Library
            Rscript -e 'install.packages(c("callr", "clipr", "fs", "glue", "rstudioapi", "shiny", "spelling", "stringr", "testthat", "withr"), repos = "https://cran.rstudio.com/")'
      - run:
          name: Install package dependencies
          command: |
            mkdir -p ~/R/Library
            Rscript scripts/install-r-pkgs.R
      # Hot fix required b/c getOption("repos") is set to older MRAN mirror
      - run:
          name: Install latest version of git2r
          command: Rscript -e 'install.packages("git2r", repos = "https://cran.rstudio.com/", dependencies = FALSE)'
      - run:
          name: Install rmarkdown 1.7 and knitr 1.18
          command: |
            Rscript -e 'remotes::install_version("rmarkdown", "1.7", repos = "https://cran.rstudio.com/")'
            Rscript -e 'remotes::install_version("knitr", "1.18", repos = "https://cran.rstudio.com/")'
      - run:
          name: Session information and installed package versions
          command: Rscript scripts/session-info.R
      - run:
          name: Build and check
          command: bash scripts/check.sh
      - store_artifacts:
          path: /tmp/workflowr/workflowr.Rcheck/
      - save_cache:
          key: r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
          paths:
            - "~/R/Library"
  pandoc_one:
    environment:
      NOT_CRAN: true
      PANDOC: 1.19
      R_LIBS: ~/R/Library
    docker:
      - image: rocker/r-base:latest
    steps:
      - restore_cache:
          keys:
            - r-pkg-cache-v3-latest-{{ arch }}-{{ .Branch }}
            - r-pkg-cache-v3-latest-{{ arch }}-
      - checkout
      - run:
          name: Install system dependencies
          command: bash scripts/install-system.sh
      - run:
          name: Install pandoc
          command: bash scripts/install-pandoc.sh $PANDOC
      - run:
          name: Clone workflowr
          command: git clone https://github.com/jdblischak/workflowr.git /tmp/workflowr
      - run:
          name: Install package dependencies
          command: |
            mkdir -p ~/R/Library
            Rscript scripts/install-r-pkgs.R
      - run:
          name: Session information and installed package versions
          command: Rscript scripts/session-info.R
      - run:
          name: Build and check
          command: bash scripts/check.sh
      - store_artifacts:
          path: /tmp/workflowr/workflowr.Rcheck/
      - save_cache:
          key: r-pkg-cache-v3-latest-{{ arch }}-{{ .Branch }}
          paths:
            - "~/R/Library"
  rmd_1.7:
    environment:
      NOT_CRAN: true
      PANDOC: 2.7
      R_LIBS: ~/R/Library
    docker:
      - image: rocker/r-base:latest
    steps:
      - restore_cache:
          keys:
            - r-pkg-cache-v3-latest-{{ arch }}-{{ .Branch }}
            - r-pkg-cache-v3-latest-{{ arch }}-
      - checkout
      - run:
          name: Install system dependencies
          command: bash scripts/install-system.sh
      - run:
          name: Install pandoc
          command: bash scripts/install-pandoc.sh $PANDOC
      - run:
          name: Clone workflowr
          command: git clone https://github.com/jdblischak/workflowr.git /tmp/workflowr
      - run:
          name: Install package dependencies
          command: |
            mkdir -p ~/R/Library
            Rscript scripts/install-r-pkgs.R
      - run:
          name: Install knitr 1.18 and rmarkdown 1.7
          # For some reason install_version() was failing with https://cloud.r-project.org
          # but it works with https://cran.r-project.org
          command: |
            Rscript -e 'remotes::install_version("knitr", "1.18", upgrade = FALSE, repos = "https://cran.r-project.org")'
            Rscript -e 'remotes::install_version("rmarkdown", "1.7", upgrade = FALSE, repos = "https://cran.r-project.org")'
      - run:
          name: Session information and installed package versions
          command: Rscript scripts/session-info.R
      - run:
          name: Build and check
          command: bash scripts/check.sh
      - store_artifacts:
          path: /tmp/workflowr/workflowr.Rcheck/
      - save_cache:
          key: r-pkg-cache-v3-latest-{{ arch }}-{{ .Branch }}
          paths:
            - "~/R/Library"
  callr_3.3.0:
    docker:
      - image: continuumio/miniconda3:4.7.10
    environment:
      NOT_CRAN: true
    steps:
      - checkout
      - run:
          name: Unset CircleCI's forced conversion of HTTPS->SSH
          command: git config --global --unset "url.ssh://git@github.com.insteadof"
      - run:
          name: Configure conda
          command: |
            conda config --set always_yes yes
            conda config --set quiet yes
            conda config --set changeps1 no
            conda config --set auto_update_conda no
            conda config --add channels defaults
            conda config --add channels conda-forge
      - run:
          name: Install
          command: |
            conda update --all
            conda config --env --add pinned_packages conda-forge::r-callr=3.3.0
            conda install qpdf \
                          r-base \
                          r-callr==3.3.0 \
                          r-clipr \
                          r-covr \
                          r-devtools \
                          r-miniui \
                          r-recommended \
                          r-shiny \
                          r-spelling \
                          r-testthat \
                          r-withr \
                          r-workflowr
      - run:
          name: Clone workflowr
          command: git clone https://github.com/jdblischak/workflowr.git ../workflowr
      - run:
          name: Session information and installed package versions
          command: |
            Rscript -e 'devtools::session_info()'
            conda list
      - run:
          name: Build and check package
          command: |
            cd ../workflowr
            bash build.sh --no-manual
      - store_artifacts:
          path: ../workflowr/workflowr.Rcheck/
  speed:
    environment:
      PANDOC: 2.7
      R_LIBS: ~/R/Library
    docker:
      - image: rocker/r-ver:3.6.1
    steps:
      - restore_cache:
          keys:
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
            - r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-
      - checkout
      - run:
          name: Install system dependencies
          command: bash scripts/install-system.sh
      - run:
          name: Install pandoc
          command: bash scripts/install-pandoc.sh $PANDOC
      - run:
          name: Clone workflowr
          command: git clone https://github.com/jdblischak/workflowr.git /tmp/workflowr
      - run:
          name: Install package dependencies
          command: |
            mkdir -p ~/R/Library
            Rscript scripts/install-r-pkgs.R
      - run:
          name: Install bench package
          command: Rscript -e 'install.packages("bench")'
      - run:
          name: Install workflowr
          command: R CMD INSTALL --build /tmp/workflowr
      - run:
          name: Install dev version of git2r
          command: Rscript -e 'remotes::install_github("ropensci/git2r")'
      - run:
          name: Record speed of wflow_publish() for dev versions of workflowr and git2r
          command: Rscript scripts/speed-test.R
      - run:
          name: Install workflowr 1.4.0
          command: Rscript -e 'remotes::install_version("workflowr", "1.4.0")'
      - run:
          name: Install git2r 0.26.1
          command: Rscript -e 'remotes::install_version("git2r", "0.26.1")'
      - run:
          name: Record speed of wflow_publish() for workflowr 1.4.0 and git2r 0.26.1
          command: Rscript scripts/speed-test.R
      - save_cache:
          key: r-pkg-cache-v3-{{ .Environment.CIRCLE_JOB }}-{{ arch }}-{{ .Branch }}
          paths:
            - "~/R/Library"
  git_time:
    environment:
      R_LIBS: ~/R/Library
    docker:
      - image: rocker/r-base:latest
    steps:
      - checkout
      - run:
          name: Install system dependencies
          command: bash scripts/install-system.sh
      - run:
          name: Install R packages
          command: |
            mkdir -p ~/R/Library
            Rscript -e 'install.packages(c("fs", "remotes"))'
            Rscript -e 'remotes::install_github("ropensci/git2r")'
      - run:
          name: Configure R setup
          command: |
            echo TZ=America/New_York >> ~/.Renviron # set time zone
            echo 'options(warn=2)' >> ~/.Rprofile # Treat warnings as errors
      - run:
          name: Record time and timezone
          command: Rscript -e 'Sys.time(); Sys.timezone()'
      - run:
          name: Test conversion of git_time to date
          command: Rscript scripts/test-git-time.R

workflows:
  version: 2
  commit:
    jobs:
      - git_time
      - speed
#  hourly:
#    triggers:
#      - schedule:
#          # "At minute 0"
#          # https://crontab.guru/#0_*_*_*_*
#          cron: "0 * * * *"
#          filters:
#            branches:
#              only:
#                - master
#    jobs:
#      - git_time
  weekly:
    triggers:
      - schedule:
          # "At 09:00 on Monday."
          # https://crontab.guru/#0_09_*_*_1
          cron: "0 09 * * 1"
          filters:
            branches:
              only:
                - master
    jobs:
      - future
      - devel
      - r361
      - r353
      - r325
      - pandoc_one
      - rmd_1.7
      - callr_3.3.0
      - speed
      - git_time
